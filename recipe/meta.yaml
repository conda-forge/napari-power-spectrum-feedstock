{% set name = "napari-power-spectrum" %}
{% set version = "0.0.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/napari-power-spectrum-{{ version }}.tar.gz
  sha256: 62e86c49c6c4eae9ad314eda2cc0f98ba3512326e20adb9065560ce7b7df8b41

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 1

requirements:
  host:
    - pip
    - python {{ python_min }}
  run:
    - magicgui
    - napari
    - numpy
    - python >={{ python_min }}
    - qtpy

test:
  imports:
    - napari_power_spectrum
  commands:
    - pip list
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/GiorgiaTortora/napari-power-spectrum
  summary: A simple plugin to get the power spectrum of frames of a stack image
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - GiorgiaTortora
    - goanpeca
    - jaimergp
